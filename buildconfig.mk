# Default build configuration for shellsentry
# Override any of these via environment variables or by editing this file locally.

NAME ?= shellsentry
INSTALL_PREFIX ?= $(HOME)
INSTALL_BINDIR ?= $(INSTALL_PREFIX)/.local/bin
