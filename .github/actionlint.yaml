---
# Custom self-hosted runner labels used by org-3leaps named runners.
# Without these, actionlint rejects otherwise-valid runs-on values.
self-hosted-runner:
  labels:
    - ubuntu-latest-arm64-s
    - windows-latest-arm64-s
